{
  "name": "wobsite",
  "scripts": {
    "serve": "SHOW_DRAFTS=true npx @11ty/eleventy --serve",
    "publish": "rm -rf _site/ && npx @11ty/eleventy && now --prod deploy _site/ -n wobsite"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.10.0",
    "eleventy-plugin-description": "^0.1.2",
    "html-to-text": "^5.1.1",
    "reading-time": "^1.2.0"
  }
}
